# Plan: Vault-content transclusion (FR-12)

| | |
|---|---|
| Source | `docs/adr/0007-vault-transclusion.md`, `SPEC.md` FR-12, `docs/competitive-analysis.md` §4 P2b |
| Branch | `feat/vault-transclusion` |
| Test command | `npm run build && npm run lint && npm test` |
| Coder model | sonnet |

TDD where the target is a pure domain function (Task 1: vitest, red before green). `src/ui/transclusion-modal.ts` and the changed half of `src/ui/copy.ts` are Obsidian-API-coupled glue with zero unit-test coverage by repo convention (confirmed: `tests/mocks/obsidian.ts` fakes only `Notice`/`TFile`/`Plugin`, no `App`/`MetadataCache`/`Vault`); Tasks 2-4 use scripted manual smoke in a dev vault instead.

## Tasks

- [ ] **Task 1 — Pure wikilink detection and single-pass body assembler (TDD)**
  **Goal:** the one place FR-12.6's "resolved/inserted text is never re-parsed" invariant is actually enforced. Detection and assembly are pure, Obsidian-free, and exhaustively vitest-covered per ADR-0007's chosen design (single-pass span merge over the pristine original body, not two sequential regex passes).
  **Files:** create `tests/transclusion.test.ts` (write first, confirm red); create `src/domain/transclusion.ts` (implement to green); modify `src/domain/placeholders.ts` (additive only: new `PlaceholderMatch` interface and `matchPlaceholders(body): PlaceholderMatch[]` export; `parsePlaceholders`/`resolvePlaceholders`'s existing signatures, behavior, and exports do not change); modify `tests/placeholders.test.ts` (add one new `describe("matchPlaceholders")` block; every pre-existing case in the file must still pass unmodified, run `npm test` to confirm before moving on).
  **Contract:**
  - `interface Wikilink { raw: string; target: string; alias: string | null; isEmbed: boolean; hasSubReference: boolean }`.
  - `detectWikilinks(body: string): Wikilink[]` — regex `!?\[\[...\]\]` mirroring `PLACEHOLDER_RE`'s documented style: target excludes `[`, `]`, `|`, `#`, `^` so nested/unclosed constructs behave exactly like `{{a{{b}}}}` already does (only the innermost well-formed link matches); a `#heading` or `^block` suffix sets `hasSubReference: true` unconditionally and such links are never looked up, resolution is entirely Task 2's job; a target that trims to `""` is not returned (mirrors `parseSegments`'s empty-name rejection). Returns **one entry per occurrence**, not deduplicated by target, this is deliberate (an accurate total-size warning needs the true insertion count); callers dedupe by `target` themselves.
  - `assembleBody(body: string, linkContents: Map<string, string>, variableValues: Record<string, string>): string` — computes wikilink spans (own internal scan of `body`) and placeholder spans (via `matchPlaceholders(body)`) over the same untouched `body`, merges both lists by start offset, and does one linear slice-and-join pass. A wikilink span becomes `linkContents.get(target)` when present, otherwise stays verbatim (covers missing-note and `hasSubReference` uniformly, since neither is ever in the map). A placeholder span becomes `variableValues[name]` when the key exists (`Object.prototype.hasOwnProperty` check, identical semantics to today's `resolvePlaceholders`), otherwise stays verbatim. Overlapping spans (a link nested inside a placeholder's pipe-segment, or vice versa, both legal since `{}`/`[]` are disjoint) resolve by "whichever span starts first wins and consumes the nested one," symmetric in both directions.
  - Remember `noUncheckedIndexedAccess`: regex match-group indexing (`m[1]`, `m[2]`, ...) is `string | undefined`; follow the exact `?? ""` / explicit-check idiom already used in `placeholders.ts`, do not add non-null assertions where a real `undefined` case exists.
  **Test (vitest, exhaustive, mirroring `tests/placeholders.test.ts`'s style):**
  - the three syntaxes: `[[target]]`, `[[target|alias]]` (alias captured but display-only, does not affect resolution lookup), `![[target]]` (isEmbed true, treated identically to `[[target]]` for resolution purposes);
  - `[[note#heading]]` and `[[note^block]]` → `hasSubReference: true`, present in both alone and combined with an alias;
  - malformed: `[[]]`, `[[   ]]` → not returned; nested `[[a[[b]]]]` → only the innermost `[[b]]` matches;
  - duplicate targets: `[[a]] text [[a]]` → two entries, same `target`, both `raw` identical;
  - `assembleBody`: a resolved link is spliced in at the right position; an unresolved link (absent from `linkContents`) stays verbatim; a placeholder value is spliced in alongside a resolved link in the same pass;
  - **the two regression-proving cases that justify the single-pass design over a two-pass one**: (a) a resolved link's content contains the literal text `{{name}}` where `name` is a key present in `variableValues`, assert the transcluded `{{name}}` text is **not** substituted, it must appear verbatim in the output; (b) `variableValues` contains a value equal to the literal text `[[target]]` where `target` is a key present in `linkContents`, assert the output contains that literal bracketed text unchanged, not the resolved content, proving a placeholder value is never re-scanned for links;
  - the overlap tie-break: a placeholder whose hint segment contains a `[[link]]`-shaped substring resolves as one placeholder replacement, the nested bracket text does not separately reappear; and the mirror case, a link whose alias contains a `{{placeholder}}`-shaped substring resolves as one link replacement;
  - a body with zero wikilinks and one or more placeholders: `assembleBody(body, new Map(), values)` produces byte-identical output to `resolvePlaceholders(body, values)` for the same inputs (proves no behavior change on the pre-existing fast path).

- [ ] **Task 2 — Vault-side resolution and the preview modal (UI layer)**
  **Goal:** turn detected wikilinks into real content (or a documented "unresolved" outcome) using only the official metadata-cache API, and give the user a confirm/cancel preview before anything is inserted (FR-12.1, FR-12.5).
  **Files:** create `src/ui/transclusion-modal.ts` (new: `resolveWikilinks`, `TransclusionPreviewModal`); modify `styles.css` (add a `.promptbox-transclusion` rule set: row list, size text, a warning line reusing the existing `--text-warning` variable already used by `.promptbox-item__warning`; reuse `.promptbox-modal`/`.promptbox-modal--wide` for the container, no new modal-chrome CSS needed).
  **Contract:**
  - `async function resolveWikilinks(app: App, sourcePath: string, links: Wikilink[]): Promise<{ resolved: Map<string, string>; unresolved: string[] }>` — for each **unique**, non-`hasSubReference` `target` among `links`: `app.metadataCache.getFirstLinkpathDest(target, sourcePath)`; a hit reads `stripFrontmatter(await app.vault.cachedRead(file))` (import unmodified from `src/storage/frontmatter.ts`) into `resolved`; a miss adds `target` to `unresolved`. Every `hasSubReference` target (deduplicated) is also added to `unresolved` (ADR-0007's adopted assumption: heading/block refs are reported in the same "unresolved" bucket as missing notes; flagged there for human review, do not silently reinterpret this at implementation time, if it needs to change, that is a plan/ADR edit, not a coder judgment call).
  - `class TransclusionPreviewModal extends Modal` — constructor `(app: App, rows: Array<{ target: string; occurrences: number; size: number }>, totalSize: number, onConfirm: () => void)`. Renders one row per unique target (path/name, occurrence count, per-occurrence size in characters), a total line, and, only when `totalSize > 50000`, a warning line (informational, never disables the confirm button). Buttons: Confirm labeled **"Continue"** (not "Copy", since confirming here does not always immediately copy, a variable-filling modal can still follow) calls `onConfirm()` then `this.close()`; Cancel just calls `this.close()`, nothing else, mirroring `VariableModal`'s "cancel copies nothing" contract extended to "and resolves nothing further."
  **Test (manual smoke, dev vault, no vitest, per the testing-boundary convention above):**
  1. A prompt body with one resolvable `[[existing-note]]`: opening the modal shows exactly one row, correct target and character count; Continue proceeds, Cancel leaves the clipboard untouched and opens no further modal.
  2. A body linking a note whose content exceeds 50,000 characters (or the same link repeated enough times to cross that total): the warning line appears; Continue is still clickable.
  3. A body with `[[missing-note]]` only (no note by that name in the vault): the modal never opens at all (zero resolvable links, FR-12.5), confirmed separately in Task 3's smoke pass together with the Notice behavior.

- [ ] **Task 3 — Wire the orchestration into `copyWithVariables`**
  **Goal:** implement FR-12's end-to-end sequencing (links resolve first, preview if applicable, then the existing placeholder flow, unchanged for bodies with no wikilinks) inside the single existing entry point, with the one narrowly-scoped signature change ADR-0007 calls out, and thread it through both copy entry points.
  **Files:** modify `src/ui/copy.ts` (`copyWithVariables` gains a required fourth parameter `sourcePath: string`; `copyRaw` is not touched at all); modify `src/ui/library-view.ts` (its one `copyWithVariables(...)` call gains `prompt.path` as a fourth argument); modify `src/ui/quick-picker.ts` (same, its one non-raw-mode `copyWithVariables(...)` call gains `prompt.path`). Grep confirms these are the only two call sites of `copyWithVariables` in `src/`; `copyRaw`'s two call sites (same two files) are unaffected.
  **Contract:** `copyWithVariables(app, title, body, sourcePath): void` keeps a synchronous, `void`-returning exported signature (no call site needs a new `void` prefix); its body becomes `void run()` where `run` is a new internal `async` function, the same fire-and-forget idiom `main.ts`'s `exportPrompts` call already uses. Inside `run`: `detectWikilinks(body)` → `resolveWikilinks(app, sourcePath, links)` → if `resolved.size > 0`, open `TransclusionPreviewModal` first, its `onConfirm` proceeds to the existing `parsePlaceholders(body)` step (unchanged call, unchanged input, the original body); if `resolved.size === 0`, proceed to that same step directly (no modal, matching FR-12.5's "zero new UI" for a body with no *resolvable* links, even if it has unresolvable ones). From there: zero placeholders → `writeClipboard(assembleBody(body, resolved, {}), title)` immediately; one or more → open the existing `VariableModal` unchanged, on submit `writeClipboard(assembleBody(body, resolved, values), title)`. After a successful clipboard write (not before, and not on a cancelled preview), fire one additional `new Notice(...)` naming every entry in `unresolved`, only when that list is non-empty.
  **Test (manual smoke, dev vault, both copy entry points exercised per repo convention):**
  1. Library view "Copy with variables" on a body with one resolvable link and one placeholder: preview appears, Continue opens the variable modal, submitting copies the assembled text with the linked content inlined and the placeholder substituted.
  2. Quick picker, same body: identical result (proves the wiring is not accidentally view-specific).
  3. A body with `[[missing-note]]` and no placeholders: no preview modal, an immediate copy, clipboard contains `[[missing-note]]` verbatim, and one Notice names it, right after the "Copied: ..." notice.
  4. A body mixing one resolvable link and one heading-reference link `[[note#Section]]`: preview lists only the resolvable one; after Continue and copy, the Notice names the heading-reference target, and the clipboard contains `[[note#Section]]` verbatim.
  5. Cancel the preview modal on a body with a resolvable link: confirm nothing is copied and no Notice of any kind fires.

- [ ] **Task 4 — Non-regression and acceptance-criteria verification pass**
  **Goal:** confirm the parts of the flow that must not change did not change, and that `SPEC.md` §3's five acceptance criteria pass literally, in one dedicated verification pass (no new code).
  **Files:** none.
  **Test (manual smoke, scripted against `SPEC.md` §3):**
  - non-regression: `copyRaw` on a body containing `[[any-link]]` and `{{any-placeholder}}` copies both completely verbatim, byte-identical to pre-feature behavior, no Notice, no modal (FR-12.7); a body with zero wikilinks (placeholders present or not) copies with the exact same UI and clipboard output as before this feature existed (FR-12.5's last bullet);
  - (a) body `Context:\n[[style-guide]]\n\nTask: {{task}}` with an existing `style-guide.md` note: preview shows one link, correct target, correct size; Continue opens the variable modal for `task` only; the clipboard contains the style guide's body inlined plus the substituted task;
  - (b) `[[missing-note]]` alone stays verbatim in the clipboard and exactly one Notice names it;
  - (c) a transcluded note that itself contains `[[other]]` keeps that inner link verbatim in the final clipboard output (depth cap 1, no recursive resolution);
  - (d) copy raw yields the body byte-identical, links and placeholders both untouched;
  - (e) a prompt with no wikilinks copies with zero new UI, confirmed on both the library view and the quick picker entry points.

- [ ] **Task 5 — Full regression gate and closeout**
  **Goal:** confirm the change set is contract-neutral outside the two intentionally-changed call sites, the full suite is green, and the roadmap and ADR index reflect the completed feature.
  **Files:** modify `docs/adr/README.md` (add the `0007-vault-transclusion` index row, following the existing table format); modify `PROJECT.md` (check off "vault-content transclusion" under Phase 1.5 with a completion date, matching the convention already used for the three preceding Phase 1.5 entries).
  **Test:** run `npm run build && npm run lint && npm test` (typecheck, production build, eslint, and the *entire* vitest suite, not only the new file). Confirm `tests/draft.test.ts`, `tests/indexer.test.ts`, `tests/perf.test.ts`, `tests/placeholders.test.ts`, `tests/prompt.test.ts`, `tests/query.test.ts`, `tests/slug.test.ts`, `tests/transfer.test.ts`, and the new `tests/transclusion.test.ts` all pass. Re-confirm by grep that `copyWithVariables(` has exactly two call sites in `src/` (`library-view.ts`, `quick-picker.ts`), both now passing four arguments, and that `copyRaw(` still has exactly two, both unchanged, no other caller of either function exists anywhere in `src/`.

## Out of scope (per `SPEC.md` §5)

Recursive resolution (depth > 1); resolving `[[note#heading]]`/`[[note^block]]` to partial content (they stay verbatim, reported as unresolved); collecting or substituting placeholders found inside transcluded content; Templater interop (NG-7).
